<p id="notice"><%= notice %></p>

<div class="container-fluid">
  <div class="row">
    <div class="col-md-4"></div>
    <div class="col-md-4">
      <%= audio_tag(@upload.file_name, autoplay: false, controls: true)  %>

      <p class="jumbotron"> <% @upload.job.get_file %></p>
    </div>
  </div>
</div>

<%= link_to 'Edit', edit_upload_path(@upload) %> |
<%= link_to 'Back', uploads_path %>
